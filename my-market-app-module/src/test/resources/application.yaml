server.port: 0

spring:
  datasource:
    url: jdbc:h2:mem:testdb/db/data;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH
    driverClassName: org.h2.Driver
    username: admin
    password: admin
  liquibase:
    contexts: no
  cache:
    type: none