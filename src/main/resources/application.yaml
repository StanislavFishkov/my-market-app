server.port: 8080

spring:
  application:
    name: my-market-app
  datasource:
    url: jdbc:h2:file:${project.dir:.}/db/data;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH
    driverClassName: org.h2.Driver
    username: admin
    password: admin
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    contexts: testdata-init
  sql:
    init:
      mode: never
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

logging:
  level:
    root: INFO
    org:
      springframework:
        web:
          client:
            RestTemplate: INFO
        orm:
          jpa: INFO
      hibernate:
        orm:
          jdbc:
            bind: trace